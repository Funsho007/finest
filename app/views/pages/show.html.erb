<div class="container">
  <% if @page %>
    <h1><%= @page.title %></h1>
    <div><%= @page.content.html_safe %></div>

    <% if @page.permalink == 'contact' %>
      <%= render 'pages/contact_form', contact: @contact %>
    <% end %>
  <% else %>
    <p>Page not found</p>
  <% end %>
</div>
